
#include <stdio.h>
#include "snakeonmeteor.h"

void generateSnake(Snake *S){
    address Head;
    AlokasiSnake(&Head);
    InfoHead(*S)=-999
	Tail(*S) = Nil;
}

void geserSnake(char input,POINT *p){
    if (input=='a'){
        Geser(p,-1,0);
    }
    else if (input=='w'){
        Geser(p,0,1);
    }
    else if (input=='s'){
        Geser(p,0,-1);
    }
    else if (input=='d'){
        Geser(p,1,0);
    } else{
        r
    }
}